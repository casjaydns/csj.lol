PORT=2550
URLHOST="localhost"
NODE_ENV="production"
MONGODB_URI="mongodb://localhost:27017/url"

# Alternative environment variable names (URLHOST takes precedence)
# HOST="localhost"
# DOMAIN="localhost"
# HOSTNAME="localhost"

# Project name (used in titles, branding, etc.)
PROJECT_NAME="shrtnr"
PROJECT_REPO="https://github.com/casjaydns/shrtnr"
